{
  "name": "webhook-discord-bot",
  "version": "1.0.0",
  "description": "Render and Vercel Webhook Discord Bot",
  "main": "app.ts",
  "repository": "https://github.com/render-examples/webhook-discord-bot",
  "author": "Render Developers",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "pnpm exec tsc",
    "start": "node dist/app.js",
    "dev": "tsnd --respawn app.ts",
    "test": "rm -rf .test-dist && tsc --outDir .test-dist && node --test \".test-dist/**/*.test.js\"",
    "typecheck": "tsc --noEmit --pretty"
  },
  "keywords": [],
  "dependencies": {
    "discord.js": "^14.18.0",
    "express": "^5.0.1",
    "standardwebhooks": "^1.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.3"
  }
}
